# V35 — measured against production, 24 Aug 2026: every header below is served
# exactly as written EXCEPT Strict-Transport-Security. Cloudflare applies its
# own HSTS setting and overrides this line — the site answers with
# `max-age=2592000` (30 days) and no `includeSubDomains`. This file is not
# wrong, it is outranked; the fix is in the Cloudflare dashboard, under
# SSL/TLS → Edge Certificates → HSTS. Re-measure with:
#   curl -sI https://app.dominicdapice.com/ | grep -i strict-transport
/*
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(self)
  Content-Security-Policy: default-src 'self'; script-src 'self' 'wasm-unsafe-eval' 'sha256-mqPXBS3QHeQk4jjTXwNUaDOa+WArc2bRzkkhb2+B1iY='; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; object-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
