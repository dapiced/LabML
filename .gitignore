node_modules
dist
coverage
playwright-report
test-results
.wrangler
*.local
.DS_Store
.lighthouse
.lighthouseci

# V27: the local language model is fetched at deploy time, never committed.
public/llm/

# V30: the Node bench keeps a flat copy of the same pinned weights here.
.llm-cache/

# V31: scratch space for vision-bench image acquisition.
.vision-bench/
